// Shared credit cost for AI features that work against contracts.
// Lives in its own file so client components can import without
// pulling in server-only Anthropic code.

export const EXTRACTION_CREDIT_COST = 3;
